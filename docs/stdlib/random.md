# Random Module Reference

Documentation automatically generated from `random.prm`.

## `seed`
**Signature:** `func void seed(int n)`  

Providing the same seed will produce the same sequence of numbers.

---

## `get_seed`
**Signature:** `func int get_seed()`  

Returns the last seed that was manually set

---

## `random`
**Signature:** `func double random()`  

Returns a random float between 0.0 and 1.0

---

## `randint`
**Signature:** `func int randint(int min, int max)`  

Returns a random integer between min and max (inclusive)

---

## `uniform`
**Signature:** `func double uniform(double min, double max)`  

Returns a random float between min and max

---

## `choice_bool`
**Signature:** `func bool choice_bool()`  

Returns a random boolean

---
